# 项目概览
我在做一个网页小游戏。具体玩法我写在 `docs/规划.md` 里了。

## 技术栈
- 纯 HTML/CSS/JavaScript，不用任何框架
- 不使用 Canvas，用 DOM 元素 + CSS 动画来做游戏画面
- 只需要一个 HTML 文件，内嵌 `<style>` 和 `<script>`（简单起见）

## 当前进度
- 项目刚刚开始，只有一个空文件夹

## 我的偏好
- 变量和函数用英文命名，注释用中文
- 代码要有清晰的结构，不要把所有东西写在一个大函数里
- UI 风格：简洁明快，浅色背景，卡通化元素